import React from "react";

import { CompletedDelivering } from "../components/Delivering/CompletedDelivering";
import { Layout } from "../components/Layout/Layout";

export const CompletedDeliveringPage = () => {
  return (
    <Layout>
      <CompletedDelivering />
    </Layout>
  );
};
